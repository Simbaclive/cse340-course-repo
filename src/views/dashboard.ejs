<%- include('./partials/header') %>
<main class="container">
    <div class="dashboard-card">
        <p>Welcome back, <strong><%= accountData.name %></strong>.</p>
        <p>You are logged in as: <strong><%= accountData.role %></strong></p>
    </div>
</main>
<%- include('./partials/footer') %>