<%- include('partials/header') %>
<main class="container">
    <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
        <ul class="error-list">
            <% errors.forEach(error => { %>
                <li><%= error.msg %></li>
            <% }) %>
        </ul>
    <% } %>
    <form action="/edit-category/<%= category && category.category_id ? category.category_id : '' %>" method="POST">
        <div class="form-group">
            <label for="category_name">Category Name:</label>
            <input type="text" id="category_name" name="category_name" required maxlength="100" value="<%= typeof category !== 'undefined' && category.category_name ? category.category_name : '' %>">
        </div>
        <button type="submit" class="btn">Update Category</button>
    </form>
    <a href="/categories">Back to Categories</a>
</main>
<%- include('partials/footer') %>